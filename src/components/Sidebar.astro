<nav class="sidebar-nav">
  <a href="/" class="s-link">Beranda</a>

  <!-- PROFIL -->
  <div class="s-group">
    <button class="s-group-btn" onclick="toggleGroup(this)">
      Profil <span class="s-chevron">▾</span>
    </button>
    <div class="s-group-items">
      <a href="/profil/sejarah" class="s-item">Sejarah Singkat</a>
      <a href="/profil/visi-misi" class="s-item">Visi Misi dan Tujuan</a>
      <a href="/profil/hymne" class="s-item">Hymne ELDUMA</a>
      <a href="/profil/arti-logo" class="s-item">Arti Logo</a>
      <a href="/profil/guru-staff" class="s-item">Guru dan Staff</a>
    </div>
  </div>

  <!-- AKADEMIK -->
  <div class="s-group">
    <button class="s-group-btn" onclick="toggleGroup(this)">
      Akademik <span class="s-chevron">▾</span>
    </button>
    <div class="s-group-items">
      <a href="/akademik/siakad" class="s-item">SIAKAD</a>
      <a href="/akademik/kurikulum" class="s-item">Kurikulum</a>
      <a href="/akademik/sekolah-pk" class="s-item">Sekolah PK</a>
    </div>
  </div>

  <!-- KESISWAAN -->
  <div class="s-group">
    <button class="s-group-btn" onclick="toggleGroup(this)">
      Kesiswaan <span class="s-chevron">▾</span>
    </button>
    <div class="s-group-items">
      <a href="/kesiswaan/osis" class="s-item">OSIS</a>
      <a href="/kesiswaan/ekskul" class="s-item">Ekskul</a>
      <a href="/kesiswaan/tata-tertib" class="s-item">Tata Tertib</a>
      <a href="/kesiswaan/prestasi" class="s-item">Prestasi</a>
    </div>
  </div>

  <a href="/sarpras" class="s-link">Sarpras</a>
  <a href="/lulusan" class="s-link">Lulusan</a>

  <!-- INFORMASI -->
  <div class="s-group">
    <button class="s-group-btn" onclick="toggleGroup(this)">
      Informasi <span class="s-chevron">▾</span>
    </button>
    <div class="s-group-items">
      <a href="/informasi/berita" class="s-item">Berita</a>
      <a href="/informasi/pengumuman" class="s-item">Pengumuman</a>
      <a href="/informasi/artikel" class="s-item">Artikel</a>
      <a href="/informasi/agenda" class="s-item">Agenda</a>
      <a href="/informasi/galeri" class="s-item">Galeri</a>
      <a href="/informasi/kontak" class="s-item">Kontak</a>
    </div>
  </div>

  <a href="/unduhan" class="s-link">Unduhan</a>

  <a href="/spmb" class="s-link s-spmb">SPMB</a>
</nav>

<style>
  .sidebar-nav {
    display: flex;
    flex-direction: column;
    gap: 0.2rem;
  }

  /* ── Link langsung ── */
  .s-link {
    display: block;
    padding: 0.65rem 0.875rem;
    border-radius: 0.6rem;
    font-size: 0.875rem;
    font-weight: 500;
    color: var(--text-secondary);
    text-decoration: none;
    transition: background 0.2s, color 0.2s;
  }
  .s-link:hover {
    background: rgba(14, 165, 233, 0.08);
    color: var(--accent);
  }

  /* ── SPMB ── */
  .s-spmb {
    color: var(--accent);
    font-weight: 700;
    border: 1.5px solid var(--accent);
    text-align: center;
    margin-top: 0.75rem;
    padding: 0.7rem 0.875rem;
  }
  .s-spmb:hover {
    background: var(--accent);
    color: #fff;
  }

  /* ── Group dengan submenu ── */
  .s-group {
    display: flex;
    flex-direction: column;
    margin-bottom: 0.15rem;
  }

  .s-group-btn {
    display: flex;
    align-items: center;
    justify-content: space-between;
    width: 100%;
    padding: 0.65rem 0.875rem;
    border-radius: 0.6rem;
    font-size: 0.875rem;
    font-weight: 500;
    color: var(--text-secondary);
    background: none;
    border: none;
    cursor: pointer;
    font-family: inherit;
    text-align: left;
    transition: background 0.2s, color 0.2s;
  }
  .s-group-btn:hover {
    background: rgba(14, 165, 233, 0.08);
    color: var(--accent);
  }
  .s-group-btn.open {
    color: var(--accent);
    background: rgba(14, 165, 233, 0.06);
  }

  .s-chevron {
    font-size: 0.65rem;
    transition: transform 0.25s;
    flex-shrink: 0;
    opacity: 0.6;
  }
  .s-group-btn.open .s-chevron {
    transform: rotate(180deg);
    opacity: 1;
  }

  /* ── Submenu items ── */
  .s-group-items {
    display: none;
    flex-direction: column;
    gap: 0.25rem;
    padding: 0.25rem 0 0.25rem 0.875rem;
    margin-top: 0.1rem;
  }
  .s-group-items.open {
    display: flex;
  }

  .s-item {
    display: block;
    padding: 0.5rem 0.75rem;
    border-radius: 0.5rem;
    font-size: 0.825rem;
    color: var(--text-muted);
    text-decoration: none;
    border-left: 2px solid var(--border-color);
    transition: background 0.2s, color 0.2s, border-color 0.2s;
  }
  .s-item:hover {
    background: rgba(14, 165, 233, 0.08);
    color: var(--accent);
    border-color: var(--accent);
  }
</style>

<script is:inline>
  function toggleGroup(btn) {
    const items = btn.nextElementSibling;
    const isOpen = items.classList.contains("open");

    document.querySelectorAll(".s-group-items.open").forEach((el) => {
      el.classList.remove("open");
      el.previousElementSibling.classList.remove("open");
    });

    if (!isOpen) {
      items.classList.add("open");
      btn.classList.add("open");
    }
  }
</script>